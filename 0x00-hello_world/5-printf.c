#include <stdio.h>

/**
 * main - Entry Point
 *
 * Description: a C program that prints with the printf funct
 *
 * Return: always 0 (success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art\,");
	return (0);
}
