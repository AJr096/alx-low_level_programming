#include "main.h"

/**
 * print_array - prnt n elemnt of array of ints
 *
 * @a: string para inut
 * @n: elemnt para input
 *
 * Return: void
 */

void print_array(int *a, int n)
{
	int count;

	for (count = 0; count < n; count++)
	{
		if (coun != (n - 1))
			printf("%d, ", a[count]);
		else
			printf("%d", a[count]);
	}
	printf("\n");
}
